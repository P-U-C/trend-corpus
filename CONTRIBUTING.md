# Contributing

Open an issue before adding or changing a theme. Fork the repo, copy the theme template, add corpus objects, then run:

```sh
make validate
make test
```

Submit a pull request with a short explanation of sources, claims, and validation results. Do not include secrets, private runtime state, personal trade context, or credentials.

